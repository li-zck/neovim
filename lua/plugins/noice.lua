return {
  "folke/noice.nvim",
  -- opts = function(_, opts)
  --   opts.presets.lsp_doc_border = true
  -- end,

  opts = {
    cmdline = {
      view = "cmdline",
    },

    presets = {
      command_palette = false,
    },

    messsages = {
      enabled = true,
    },

    popupmenu = {
      enabled = true,
    },
  },
}
