return {
  "folke/noice.nvim",
  -- opts = function(_, opts)
  --   opts.presets.lsp_doc_border = true
  -- end,

  opts = {
    cmdline = {
      -- enabled = false,
      view = "cmdline",
    },
    presets = {
      command_palette = false,
    },
    messsages = {
      enabled = false,
    },
    popupmenu = {
      enabled = true,
    },
  },
}
